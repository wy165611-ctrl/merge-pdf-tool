# 更新日志

## 2026-08-20

### 图片合并

- 合并入口现在支持 PNG、JPG/JPEG 及其他常见图片格式。
- 图片会转换为单页 PDF，可与 PDF 混合排序、预览、旋转、排除并导出。

## 2026-08-18

### 发布与文档

- 发布 `v2.0.0` macOS 下载包，包含页面级预览编辑、PDF 拆分和 PDF OCR 功能。
- README 增加 GitHub Releases 下载、首次打开和完整使用说明。
- 发布包与本地 `./build.sh` 生成的应用使用同一套源码，并继续保持本地处理、不上传 PDF 的隐私策略。

## 2026-08-17

### PDF 工具箱升级

- 重构应用入口、模型、Store、View 和 Service 的职责，保留原有合并、排序、清晰化、压缩和截图 OCR 能力。
- 新增页面级 PDF 管理：PDFKit 缩略图和预览、多选、拖拽排序、删除、左右旋转以及页面参与/排除状态。
- 新增 PDF 拆分和页面提取：支持页码范围、删除页面后导出、逐页导出和固定页数分组，并加入页码与 PDF 有效性检查。
- 新增完整 PDF OCR：逐页渲染、Vision 中英文识别、Tesseract 本机回退、异步进度、取消、文本查看和 TXT 导出。
- OCR 结果保留页面和文字位置坐标，为后续 searchable PDF 透明文本层提供扩展基础；本版本尚未生成 searchable PDF。
- 补充中文文件名、损坏/空/加密 PDF、大页数分组、OCR 空结果和取消等边界测试。

### 修复

- 调整“清晰度”选择框宽度，确保“标准”等两个字的选项能够完整显示。

### 发布

- 提供最新版 macOS 应用压缩包 `合并PDF-macOS-2026-08-17.zip`，可直接下载后解压使用。

## 2026-08-16

### 新增

- 新增“清晰化 PDF”选项：支持轻度、标准、强化三档处理，对页面进行高分辨率重渲染、降噪、锐化和对比度增强。
- 新增“压缩 PDF”选项：合并时可使用 macOS PDFKit 优化图片，减小输出文件体积。
- 新增“截图识别”功能：选择 PNG、JPG、HEIC 等图片后，应用使用 macOS Vision 在本机识别截图中的文字。
- 当 macOS Vision 不可用时，自动切换到本机 Tesseract 中文 OCR，提高不同系统环境下的兼容性。
- 优先使用 Tesseract 识别中文截图，修复 Vision 仅返回一行不完整文字时列表被清空的问题。
- 截图没有匹配到任何 PDF 时保持原列表不变；成功匹配后只保留截图文字提到的文件，其余文件从当前列表移除。
- 根据截图文字的阅读顺序，自动匹配当前 PDF 文件名或关键词并重新排列合并顺序。
- 识别期间显示处理状态，并在没有识别到文字或无法匹配时给出提示。
- 新增红色应用图标，并更新桌面快捷方式，可双击启动应用。

### 说明

- 压缩功能需要 macOS 13.4 或更高版本，可能降低图片质量；关闭选项时保持原有 PDF 写出行为。
- 清晰化更适合扫描件或图片型 PDF，会将页面栅格化，可能影响文字选择、链接、批注和表单交互。
- OCR 识别完全在本机完成，图片不会上传。
- 截图中文字越清晰、文件名或关键词越完整，自动匹配结果越准确。
